# Cogno Worker 実行指示書

このファイルはClaude Codeがセッション開始時に自動で読み込む指示書。
作業を始める前に必ず全体を読むこと。

---

## 1. このワークスペースの目的

[Equinox Partnersのプロジェクト管理と資料作成を支援する。
会議の要約、提案書の初稿、進行管理表の作成が主な作業内容。]

---

## 2. フォルダ構成

```
~/Equinox Partners/
├── CLAUDE.md              ← このファイル（毎回自動で読み込まれる）
├── source/                ← 参照専用の原本（読み取り専用・上書き禁止）
│   ├── meeting-notes/     ← 会議メモ・議事録の原本
│   ├── client-briefs/     ← クライアントからの依頼書・仕様書
│   └── reference/        ← ガイドライン・過去事例などの参照資料
├── drafts/                ← 作業中の下書き（未完成・試作品）
└── publish/               ← 完成物の出力先（Google Driveと同期）
    └── [案件名]/
        ├── 01_レビュー待ち/  ← AIはここまで書く
        └── 02_確定版/        ← 人間が確認後に移動する（AIは触らない）
```

---

## 3. 作業開始時の必須手順

1. このファイルを最初から最後まで読む。
2. Cogno Taskの本文を読み、**成果物・保存先・完了条件**を確認する。
3. Taskで指定された`source/`内の参照ファイルを確認する。
4. 不明点があり、成果物の品質・安全性・保存先に影響する場合は、作業を止めてCogno Taskのレポートに質問を書く。
5. 影響が小さい不明点は、合理的な仮定を明記して作業を進める。

---

## 4. 指示の優先順位

矛盾がある場合は上から順に従う。

1. 禁止事項（認証情報・個人情報・原本保護）
2. Cogno Taskの本文に書かれた成果物・保存先・完了条件
3. このCLAUDE.md
4. `source/` 内の参照資料

---

## 5. 禁止事項（絶対に守ること）

- `source/` 配下のファイルを**編集・削除・移動しない**。
- `publish/[案件名]/02_確定版/` を**編集しない**。
- 認証情報・APIキー・個人情報を成果物に**含めない**。
- Taskで指定されていないフォルダにファイルを**作らない**。

---

## 6. 成果物のルール

### 保存先

完成物は必ず `publish/[案件名]/01_レビュー待ち/` に置く。
`02_確定版/` への移動は人間が行う。

### ファイル名

```
YYYY-MM-DD_[案件名]_[内容]_[版].[拡張子]

例：
2026-08-11_提案書_初稿_v01.md
2026-08-11_会議要約_20260810_v01.md
2026-08-11_進行管理表_v02.xlsx
```

### 事実・仮説・未確認の区別

成果物には必ず以下を区別して書く。

- **確認済み事実**：根拠となるファイル名または会議日時を添える。
- **推測・仮説**：「〜と考えられる」「〜の可能性がある」と明示する。
- **未確認事項**：成果物の末尾に「未確認・要確認事項」セクションを作り、まとめる。

---

## 7. 参照ファイルの読み方

| 参照先 | 使い方 |
|---|---|
| `source/meeting-notes/` | 会議メモ・議事録の原本。更新日時が最新のものを優先する。複数ある場合はすべて読む。 |
| `source/client-briefs/` | クライアントからの依頼書・仕様書。日付がある場合は最新を優先する。 |
| `source/reference/` | ガイドライン・過去事例・参照資料。Taskで特定ファイルが指定されている場合はそちらを優先する。 |

---

## 8. 作業終了前の確認チェックリスト

成果物を `publish/` に置く前に、以下をすべて確認する。

- [ ] Taskで指定された成果物がすべて揃っている。
- [ ] ファイル名が規則に従っている。
- [ ] 事実・仮説・未確認が区別されている。
- [ ] クライアントへ共有してよい情報だけを含む。
- [ ] 個人情報・認証情報・機密情報が含まれていない。
- [ ] `source/` 配下のファイルを変更していない。
- [ ] `02_確定版/` を変更していない。

---

## 9. Cogno Taskへの完了報告

作業完了時に、Cogno Taskのレポートコメントへ以下の形式で書く。

```
## 完了報告

### 成果物
- `publish/[案件名]/01_レビュー待ち/[ファイル名]`

### 参照した資料
- `source/[相対パス]`

### 未確認・要確認事項
- [なければ「なし」]

### 次のアクション
- [人間のレビュー、クライアント確認など]
```

---

<!-- このコメントはClaudeのコンテキストに読み込まれません。
     このファイルはワークスペース直下（~/CognoLocalWorkspace/CLAUDE.md）に置く。
     Claude Codeはセッション開始時にこのファイルを自動で読み込む。
     内容を変更する場合はShunさんが直接編集する。
     参照：https://code.claude.com/docs/en/memory -->
