# publish/

成果物の出力先フォルダ。案件ごとにサブフォルダを作成する。

## フォルダ構成

```
publish/
└── [案件名]/
    ├── 01_レビュー待ち/   ← AIはここまで書く
    └── 02_確定版/          ← 人間が確認後に移動する（AIは触らない）
```

## 運用ルール

- AIが作成した成果物は必ず `01_レビュー待ち/` に配置する。
- `02_確定版/` への移動は担当者が確認後に行う。
- AIは `02_確定版/` を編集・削除しない。
